spring:
  profiles:
    include: oauth
  mvc:
    static-path-pattern: /resources/static/**
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
    check-template-location: true